# ============================================================================
# dsh-peak-gate —— DeepSeek 高峰避让闸门（bundle 挂载声明）
# ============================================================================
# 本文件是插件的"挂载声明"：DSH 启动时按 bundle 层栈叠加各插件 patch，
# 把 dsh-peak-gate 插入 Web profile 的配置树。运行时热装配走
# dsh-super-injector（dev_install_package），重启后由 bundles 列表正常装配。
# ============================================================================

- insert:
    - id: dsh-peak-gate
      name: dsh-peak-gate
